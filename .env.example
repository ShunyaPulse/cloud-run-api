# ─── Environment Variables ───────────────────────────────────────────
# Copy this file to .env and fill in real values (for local dev only).
# In production (Cloud Run), these are injected via GitHub Actions / Secret Manager.

# Neon Serverless Postgres connection string
DATABASE_URL=postgresql://user:password@ep-example-123456.us-east-2.aws.neon.tech/dbname?sslmode=require

# Oracle Cloud Always-Free VM Redis Cache (Optional)
# Format: redis://:<password>@<OCI_PUBLIC_IP>:6379
REDIS_URL=redis://:changeme_secure_redis_password@123.45.67.89:6379

# Server port (Cloud Run injects PORT automatically; default 8080)
PORT=8080
